<div class="container">
  <div class="product-page">
    <header class="product-header">
      <h1><%= @product.name %></h1>
      <p> $ <%= @product.price %></p>
      <p><%= @product.category.capitalize %></p>
    </header>
  </div>
</div>
